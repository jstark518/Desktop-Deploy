import {styled} from "@mui/system";
import {Paper} from "@mui/material";

export const HeaderPaperStyledContainer = styled(Paper)({
    flex: 1,
    background: "#88D3A0",
    padding: '16px',
    borderRadius: "5px",
    marginBottom: "10px",
    marginTop: "24px"
});

export const PaperStyledContainer = styled(Paper)({
    background: "#E6FEE6",
    padding: '16px',
    borderRadius: "5px",
    flex: 1
});

